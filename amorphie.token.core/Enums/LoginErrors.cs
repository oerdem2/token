namespace amorphie.token.core.Enums;

    public enum LoginErrors
{
    UserNotFound = 10,
    WrongPassword = 20,
    WrongOtp = 30,
    BlockedUser = 40,
    BlockUser = 50,
    SamePassword = 60,
    General = 100
}
