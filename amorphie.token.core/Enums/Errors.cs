namespace amorphie.token.core.Enums;

public enum Errors
{
    InvalidClient = 10,
    NotActive = 20,
    InvalidResponseType = 30,
    InvalidScopes = 40,
    InvalidAuthorizationCode = 50,
    CodeVerifierNotMatched = 60,

    InvalidUser = 70
}
